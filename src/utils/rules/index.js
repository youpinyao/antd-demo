
export const password = require('./password');

export const user_name = require('./user_name');

export const required = require('./required');

export const url = require('./url');

export const licence_sn = require('./licence_sn');

export const email = require('./email');

export const number = require('./number');

export const phone = require('./phone');
